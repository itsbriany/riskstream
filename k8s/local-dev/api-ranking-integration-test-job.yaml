apiVersion: batch/v1
kind: Job
metadata:
  name: api-ranking-integration-test
  namespace: local-dev
  labels:
    app: api-ranking-integration-test
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: api-ranking-integration-test
    spec:
      restartPolicy: Never
      containers:
        - name: pytest
          image: riskstream:local
          imagePullPolicy: Never
          env:
            - name: API_BASE_URL
              value: http://riskstream
            - name: S3_ENDPOINT
              value: minio:9000
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: root-user
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: root-password
            - name: S3_USE_SSL
              value: "false"
          command:
            - /bin/sh
            - -c
            - python -m pip install --no-cache-dir pytest && pytest -v /tests/test_api_ranking.py
          volumeMounts:
            - name: test-file
              mountPath: /tests
              readOnly: true
      volumes:
        - name: test-file
          configMap:
            name: api-ranking-integration-test-code
